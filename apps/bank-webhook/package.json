{
  "name": "bank-webhook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild --platform=node --bundle --sourcemap --outdir=dist src/index.ts",
    "dev": "nodemon --exec \"npm run build && nodemon dist/index.js\" --watch src"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@repo/db": "*",
    "@types/express": "^4.17.21",
    "dotenv": "^16.4.5",
    "esbuild": "^0.20.2",
    "express": "^4.19.2",
    "zod": "^3.22.4"
  }
}
